parameters:
  workingDirectory: ""
steps:
  - script: |
      cd ${{ parameters.workingDirectory }}

      . secrets/env

      az cosmosdb sql database delete --name $DATABASE_NAME --account-name $COSMOSDB_ACCOUNT --resource-group $RESOURCEGROUP >/dev/null

    displayName: "🧹 Delete custom RP DB"
    condition: always()
