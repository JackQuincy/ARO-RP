parameters:
  workingDirectory: ""
steps:
  - script: |
      cd ${{ parameters.workingDirectory }}

      . secrets/env

      echo "📦 Creating new DB $DATABASE_NAME"

      az group deployment create \
        -g "$RESOURCEGROUP" \
        -n "databases-development-test" \
        --template-file deploy/databases-development.json \
        --parameters \
          "databaseAccountName=$COSMOSDB_ACCOUNT" \
          "databaseName=$DATABASE_NAME" \
          >/dev/null
    displayName: "🚀 Deploy custom RP DB"
